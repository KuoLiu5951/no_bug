<p>student: <%= @apply.student.name %></p>
<p>project: <%= @apply.project.name %></p>
<p>teacher: <%= @apply.teacher.name %></p>
<p>is_published: <%= @apply.is_published %></p>

<div class="mt-4">
  <%= link_to 'Edit', edit_admin_apply_path(@apply), class: 'btn btn-success' %>
  <%= link_to 'Back', admin_applies_path, class: 'btn btn-success' %>
</div>
