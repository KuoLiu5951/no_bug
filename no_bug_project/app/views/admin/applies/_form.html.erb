<%= simple_form_for [:admin, @apply] do |f| %>
  <%= f.error_notification %>

  <p>student: <%= @apply.student.name %></p>
  <p>project: <%= @apply.project.name %></p>
  <p>teacher: <%= @apply.teacher.name %></p>

  <%= f.input :is_published, as: :boolean, checked_value: true, unchecked_value: false %>

  <%= f.button :submit %>
<% end %>
